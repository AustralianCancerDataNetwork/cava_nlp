lang: cava_nlp
components:
  weight_value:
    factory: rule_engine
    config:
      token_label: weight
      value_label: weight_value
      entity_label: WEIGHT
      token_patterns: ${rulesets.weight.token}
      value_patterns: ${rulesets.weight.value}
      norm_patterns: ${rulesets.weight.norm}
      exclusions: ${rulesets.weight.exclusions}
      merge_ents: true